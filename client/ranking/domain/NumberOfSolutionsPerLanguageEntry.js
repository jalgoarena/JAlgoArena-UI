// @flow

export class NumberOfSolutionsPerLanguageEntry {
    first: string;
    second: number;

    constructor(first: string, second: number) {
        this.first = first;
        this.second = second;
    }
}
